# import autokeras as ak
import time
from bility import shitshow
import numpy as np
# this thing is fucked.
# never mind. we will deal with it.
# use metaprogramming.
# you twist yourself till something happens.
# use system random?
# there is a big pile of shit.
# must use tensorflow as backend?
# if we do not know how it works, then everything is alright.
# just throw it.
# random data generation.
# just pure random shit. do not even expect outcome.
# this is called the karma.
# i can even render them into pictures.
# make it into the fuck.
# it does not matter.
# words are too much.
# what is the shape of it?
# this is horrible, since we do not have shit in it.
# the hyper-model?
clt = ["collective.csv", "final.csv"]
x_train, y_train = shitshow(clt[0])
x_test, y_test = shitshow(clt[1])
print(type(x_test))
x_train = np.array(x_train, dtype=np.str)
x_test = np.array(x_test, dtype=np.str)
print(type(x_test))
# from template to template.
# need to use pycuda?
# what the fuck is going on?
# # the deep hidden meaning.
# tr = ak.TextRegressor(seed=int(time.time()))
# # what the fuck?
# tr.fit(x_train, y_train, epochs=200)
# print('Accuracy: {accuracy}'.format(accuracy=clf.evaluate(x_test, y_test)))
# shit. what is machine learning anyway?
# infer from the data.
# the translator.
# the function.
# never mind.
# name of the automodel.
# fuck me.
# how to create a training set?
# what is the result?
