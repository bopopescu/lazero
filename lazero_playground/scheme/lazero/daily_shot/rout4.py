import zmq
import time
# better integrate it.
# this is fucked.
# all fucked up.
# what is wrong with you?
# is this good?
# not binding?
# oh shit!
# letting that thing to control your computer?
# greatest shit ever!
# really nigga?
# we must bind this shit?
# can we give up that binding shit?
# t0, t1 = 0, 0
# while True:
  # there is no trust.
    # msg = socket.recv_string()
    # socket.sennd_string("")
    # address in use.
    # what the fuck is going on?
    # t1 = time.time()
    # WTF?
    # dead after done.
    # we will have the shit.
    # print(msg)
    # print(t1-t0)
    # socket.send_string("client message to server1")
    # what is it anyway?
    # what makes it?
    # well the advisor could also use the hint programs.
    # that sounds like something.
    # we can do the job, right?
    # automatic search engine?
    # sounds great?
    # where is reasoning?
    # print(t1 - t0)
    # we can do real-time processing, trust me.
    # real-time processing is important, since it is my fucking intention.
    # but this can also be done using the DOM object?
    # fuck it. anyway, it sucks.
    # metaprogramming must be real time, since this is about imitation of human being.
    # and we do the real-time thing.
    # check if it is in use?
    # have conscious?
# print(dir(socket))
'''['AFFINITY', 'BACKLOG', 'BINDTODEVICE', 'CONFLATE', 'CONNECT_RID', 'CONNECT_ROUTING_ID', 'CONNECT_TIMEOUT', 'CURVE_PUBLICKEY', 'CURVE_SECRETKEY', 'CURVE_SERVER', 'CURVE_SERVERKEY', 'DELAY_ATTACH_ON_CONNECT', 'EVENTS', 'FAIL_UNROUTABLE', 'FD', 'GSSAPI_PLAINTEXT', 'GSSAPI_PRINCIPAL', 'GSSAPI_PRINCIPAL_NAMETYPE', 'GSSAPI_SERVER', 'GSSAPI_SERVICE_PRINCIPAL', 'GSSAPI_SERVICE_PRINCIPAL_NAMETYPE', 'HANDSHAKE_IVL', 'HEARTBEAT_IVL', 'HEARTBEAT_TIMEOUT', 'HEARTBEAT_TTL', 'HWM', 'IDENTITY', 'IMMEDIATE', 'INVERT_MATCHING', 'IPC_FILTER_GID', 'IPC_FILTER_PID', 'IPC_FILTER_UID', 'IPV4ONLY', 'IPV6', 'LAST_ENDPOINT', 'LINGER', 'MAXMSGSIZE', 'MCAST_LOOP', 'MECHANISM', 'MULTICAST_HOPS', 'MULTICAST_LOOP', 'MULTICAST_MAXTPDU', 'PLAIN_PASSWORD', 'PLAIN_SERVER', 'PLAIN_USERNAME', 'PROBE_ROUTER', 'RATE', 'RCVBUF', 'RCVHWM', 'RCVMORE', 'RCVTIMEO', 'RECONNECT_IVL', 'RECONNECT_IVL_MAX', 'RECOVERY_IVL', 'RECOVERY_IVL_MSEC', 'REQ_CORRELATE', 'REQ_RELAXED', 'ROUTER_BEHAVIOR', 'ROUTER_HANDOVER', 'ROUTER_MANDATORY', 'ROUTER_NOTIFY', 'ROUTER_RAW', 'ROUTING_ID', 'SNDBUF', 'SNDHWM', 'SNDTIMEO', 'SOCKS_PROXY', 'STREAM_NOTIFY', 'SUBSCRIBE', 'SWAP', 'TCP_ACCEPT_FILTER', 'TCP_KEEPALIVE', 'TCP_KEEPALIVE_CNT', 'TCP_KEEPALIVE_IDLE', 'TCP_KEEPALIVE_INTVL', 'TCP_MAXRT', 'THREAD_SAFE', 'TOS', 'TYPE', 'UNSUBSCRIBE', 'USE_FD', 'VMCI_BUFFER_MAX_SIZE', 'VMCI_BUFFER_MIN_SIZE', 'VMCI_BUFFER_SIZE', 'VMCI_CONNECT_TIMEOUT', 'XPUB_MANUAL', 'XPUB_NODROP',
'XPUB_VERBOSE', 'XPUB_VERBOSER', 'XPUB_WELCOME_MSG', 'ZAP_DOMAIN', 'ZAP_ENFORCE_DOMAIN', '__class__', '__copy__', '__deepcopy__', '__del__', '__delattr__', '__dict__', '__dir__', '__doc__', '__enter__', '__eq__', '__exit__', '__format__', '__ge__', '__getattr__', '__getattribute__', '__gt__', '__hash__', '__init__', '__init_subclass__', '__le__', '__lt__', '__module__', '__ne__', '__new__', '__pyx_vtable__', '__reduce__', '__reduce_ex__', '__repr__', '__setattr__', '__setstate__', '__sizeof__', '__str__', '__subclasshook__', '__weakref__', '_closed', '_deserialize', '_get_attr_opt', '_monitor_socket', '_poller_class', '_set_attr_opt', '_shadow', 'bind', 'bind_to_random_port', 'close', 'closed', 'connect', 'context', 'copy_threshold', 'disable_monitor', 'disconnect', 'fileno', 'get', 'get_hwm', 'get_monitor_socket', 'get_string', 'getsockopt', 'getsockopt_string', 'getsockopt_unicode', 'hwm', 'join', 'leave', 'monitor', 'poll', 'recv', 'recv_json', 'recv_multipart', 'recv_pyobj', 'recv_serialized', 'recv_string', 'recv_unicode', 'send', 'send_json', 'send_multipart', 'send_pyobj', 'send_serialized', 'send_string', 'send_unicode', 'set', 'set_hwm', 'set_string', 'setsockopt', 'setsockopt_string', 'setsockopt_unicode', 'shadow',
'socket_type', 'subscribe', 'unbind', 'underlying', 'unsubscribe']'''

# we will check it.
def useMePlease(x):
  # we will do it again.
    port = "5560"
    # what the fuck is going on?
    # this is shit.
    context = zmq.Context()
    socket = context.socket(zmq.PUB)
    # socket.connect("tcp://127.0.0.9:%s" % port)
    # morphous?
    socket.bind("tcp://127.0.0.9:%s" % port)
    socket.send_string(x)
    # socket.send_multipart([b'hello.world', x])
    # socket.
    # what the fuck?
    # only hard-core shits will do it.
    # can you really send it?
    # import it beforehand?
    # just to execute that shit?
    # can we close it?
    # make it sustain.
    # sending failed?
    # well, you can launch the broadcaster.
    # use it as the broadcaster.
    # in some way.
    # what the fuck?
    # this is great.
    # you are fucking kidding me.
if __name__ == "__main__":
    while True:
    # come on?
        time.sleep(1)
        t = "hello_world"
        # t = [1, 2, 3, 4]
        useMePlease(t)
        # fuck me.
        print(time.time())
        print(t)
        # holy shit.
    # do we need to subscribe messages from the whole shit?
    # the trust? how do we have it?
    # build from ground up or shit?
    # what the fuck is that?
    # now we are heading to the next level?
    # this is another story.
    # t1 = time.time()
    # time.sleep(1)
    # t0 = time.time()
