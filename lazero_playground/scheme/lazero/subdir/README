Warning: Never conflict with python namespace.

If having same name as a site-package, python will prefer the local package and therefore all our efforts will be in vain!

Examples:
	python3 metaCheckIII.py  sklearn > subdir/sklearn_notsame.py
	python3 metaCheckIII.py  torch > subdir/torch_notsame.py
