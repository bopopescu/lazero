var count=process.argv.length;
var counts=process.argv[2];
console.log(counts);
console.log(count);
// it returns one more than the final index
counts+="urbitch";
console.log(count++);

console.log(counts);
