def returnCore():
    # a is the code.
    a0=["\ue230","\ue241"]
    a1=[chr(ord(a0[0])+x) for x in range(10)]+[chr(ord(a0[1])+x) for x in range(6)]
    print(a1)
    # try:
    #     return a1.index(a)
    # except:
    #     return None
    # check the layout version of the fucking file. i fucking swear it is gonna be fascinating.
returnCore()
# unicode problems.
# mother fucker. this is shit. for real.
"""
Alias names are those for ISO/IEC 6429:1992. Commonly used
alternative aliases are also shown.
"""