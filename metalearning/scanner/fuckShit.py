from fuckMe import secondTest
from getTrivial import fuckMe
from simpleStorageR import storeList
# eliminate those not exist.
# conclude those not exist.
# conclude from the path.
simpleClassification=[[],[],[]]
for x in range(14166):
    bitch=fuckMe(x)
    prt=secondTest(bitch[0])
    if prt[0]==True:
        simpleClassification[0].append(bitch)
    elif prt[1]==True:
        simpleClassification[1].append(bitch)
    else:
        simpleClassification[2].append(bitch)

storeList(simpleClassification)
