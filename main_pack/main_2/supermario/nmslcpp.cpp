//#include "stdafx.h"
#include <iostream>
#include <windows.h>
#include <conio.h>
#include <winuser.h>
using namespace std;
// usually it is because of the ms-python language server.     -> chrome.
// there's nothing like autosave.
// hereby I recommend some cracked version of desktop, browser, file explorer.
// chromium, KDE, Web IDE.
int main() {
	DWORD y = GetClipboardSequenceNumber();
	while(true) {
		Sleep(1000);
		printf("%lu\n",y);
		y = GetClipboardSequenceNumber();
		// this is useful somehow.
// i can even use this shit to prank for a while.
// this is to check if there's a way to monitor system shits.
// get the code!
	}
//return 0;
// this one is a global thing.
// either find a replacement of CLIPBOARD or do networking.
// chrome is eating way too much RAM.
}
