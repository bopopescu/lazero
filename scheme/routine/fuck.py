import zmq
# import time

# this is fucked.
# all fucked up.


# why it is in use?
# check it?
# dirty bitch.
# address in use?
# how comes?
# what the fuck?
# you will have shit?
# sooner or later?
# use a broadcaster?
# what the fuck?
# can we make it?
# what the heck?
# this will be awful.
# what the fuck?
# are we going to die?
# chasing the shit!
# this won't work.
port = "5560"
# unbind the fucking endpoint?
context = zmq.Context(io_threads=1)
socket = context.socket(zmq.PUB)
socket.bind("tcp://127.0.0.7:%s" % port)
# man we cannot have shit!
# i don't believe this shit!
def jerk(nazi):
  # what the fuck?
  # well, i think sending it into a command will work?
  # breaking the fucking sytax?
    socket.send_pyobj(nazi)
    # print(nazi)
    # what the fuck?
    # socket.unbind("tcp://127.0.0.7:%s" % port)
    # fucking hell.

if __name__ == "__main__":
    # what the hell is this?
    while True:
        h = [1, 2, 3, 4]
        jerk(h)
