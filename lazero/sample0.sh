#!/bin/bash
while true
do
# cat lazero | awk '{print $1 " again"}' > lazero && sleep 1 & sleep 1 && kill $!
echo deep dark fantasy > lazero && sleep 1 & sleep 1 && kill $! 
# cannot get enough content inside.
# must wait for producers to come together.
# echo hello world
done
# can't get shit from the node.
# system will dynamically acquire the lock.
# this is the quantum mechanics.